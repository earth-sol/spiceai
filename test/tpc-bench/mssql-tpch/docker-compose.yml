services:
 mssql_tpch:
    image: ghcr.io/spiceai/spice-mssql-bench:latest
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    ports:
      - 1433:1433
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=S3cretP@ssw0rd
      - DB_NAME=tpch_sf1
    # volumes:
    #   - ./data:/data